Author: Cody DePeel
Submission Date: 11-18-2023

COMPILATION STEPS:
1. First, run "gcc -o smallsh smallsh.c"
2. Next, run "./smallsh"
    a. The shell will run and prompt the user for an input using a colon (: ).

    b. To run the grading script, instead of ./smallsh, run the following
    command:
        ./p3testscript 2>&1
    
 